<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bam00.csfinalapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FindJobActivity" />
        <activity android:name=".JobStatisticsActivity" />
        <activity android:name=".ManageDebtActivity" />
        <activity android:name=".ViewPaymentPlanActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".JobStatisticsResultsActivity" />
        <activity android:name=".AddPaymentActivity" />
        <activity android:name=".HomeActivity">
        </activity>
    </application>

</manifest>